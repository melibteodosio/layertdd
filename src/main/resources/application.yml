server:
  port: 8080
  error:
    include-stacktrace: on_param
spring:
  datasource:
    url: jdbc:h2:mem:brubank
    username: bteodosio
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
